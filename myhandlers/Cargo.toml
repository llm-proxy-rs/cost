[package]
name = "myhandlers"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.9"
handlers = { git = "https://github.com/llm-proxy-rs/cognito.git", version = "0.1.0" }
myerrors = { path = "../myerrors" }
sqlx = { version = "0.9.0", features = ["postgres", "tls-rustls"] }
tower-sessions = "0.15.0"
